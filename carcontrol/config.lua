Config = {
  UsingLoader = false,
}

Controls = {
  ["Toggle"] = false,     -- Toggle key to open the UI.                  
}                         -- Set this to false if you don't want to open via hotkey, and want to control the UI via the export functions. 
                          -- Set this to a key code if you want to open via hotkey.

mLibs = exports["meta_libs"]